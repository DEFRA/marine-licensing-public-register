version: 2

updates:
  # Update npm packages
  - package-ecosystem: npm
    directory: /
    open-pull-requests-limit: 10

    # Group packages into shared PR
    groups:
      aws-sdk:
        patterns:
          - '@aws-sdk/*'

      lint:
        patterns:
          - 'prettier'
          - 'stylelint'
          - 'stylelint-*'

      logging:
        patterns:
          - '*-pino'
          - '*-pino-format'
          - 'pino'
          - 'pino-*'

      tools:
        patterns:
          - 'husky'
          - 'vitest'
          - 'vitest-*'
          - '@vitest/*'
          - 'vite'
          - 'nodemon'
          - 'npm-run-all'

    ignore:
      - dependency-name: 'eslint-*'
      - dependency-name: 'eslint'

    # Schedule: weekly on Tuesday (Europe/London)
    schedule:
      interval: weekly
      day: tuesday
      time: '10:30'
      timezone: 'Europe/London'

    versioning-strategy: increase

    allow:
      # Include direct package.json updates
      - dependency-type: direct

  # Update GitHub Actions
  - package-ecosystem: github-actions
    directory: /

    # Schedule: weekly on Tuesday (Europe/London)
    schedule:
      interval: weekly
      day: tuesday
      time: '10:30'
      timezone: 'Europe/London'
